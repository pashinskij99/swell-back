{
  "collectionName": "components_model_models",
  "info": {
    "displayName": "model",
    "icon": "air-freshener",
    "description": ""
  },
  "options": {},
  "attributes": {
    "object": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": [
        "files"
      ]
    },
    "environment": {
      "type": "enumeration",
      "enum": [
        "sunset",
        "dawn",
        "night",
        "warehouse",
        "forest",
        "apartment",
        "studio",
        "city",
        "park",
        "lobby"
      ],
      "default": "studio",
      "required": true
    },
    "backgroundStyle": {
      "type": "string",
      "default": "radial-gradient(circle, rgba(67,67,67,1) 5%, rgba(0,0,0,1) 75%)",
      "required": true
    },
    "config": {
      "type": "json"
    }
  }
}
